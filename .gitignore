# Compiled binary (built locally or in CI; attached to GitHub Releases as an asset).
/gotopr
gotopr-darwin-*
gotopr-linux-*

# Release staging
/dist/

# Runtime state (lives in the herdr state dir, never in the repo).
state.json
prcache.json
repos.json

# macOS
.DS_Store
